{
  "name": "@stnew/ontentful-proxy",
  "version": "0.0.2",
  "description": "A contentful proxy middleware",
  "main": "dist/index.js",
  "module": "src/index.js",
  "scripts": {
    "jest": "jest --coverage --verbose --color"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-react",
      "next/babel"
    ],
    "env": {
      "test": {
        "plugins": [
          "transform-es2015-modules-commonjs"
        ]
      }
    }
  },
  "jest": {
    "setupFiles": [
      "../../jest.setup.js"
    ]
  },
  "dependencies": {
    "contentful": "^7.3.0",
    "redis": "^2.8.0"
  },
  "devDependencies": {
    "babel-jest": "^24.1.0",
    "jest": "^24.1.0"
  }
}
